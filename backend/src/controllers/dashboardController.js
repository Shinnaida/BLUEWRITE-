const s=require('../services/dashboardService'),{success,error}=require('../utils/response');exports.admin=async(req,res)=>{try{return success(res,await s.admin(req.user.id))}catch{return error(res,'Unable to load dashboard.',500)}};exports.officer=async(req,res)=>{try{return success(res,await s.officer(req.user.officer.id))}catch{return error(res,'Unable to load dashboard.',500)}};
